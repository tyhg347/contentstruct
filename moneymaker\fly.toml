runtime = "python"
[build]
  builder = "paketobuildpacks/builder-jammy-base"
  buildpacks = ["paketo-community/python"]
[deploy]
  release_command = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
